apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "common.fullname" . }}-athena-dashboard
  labels:
    {{- include "dashboard.labels" . | nindent 4 }}
  annotations:
    {{- include "dashboard.annotations" . | nindent 4 }}
data:
  aws_cur2_billing_dashboard.json: |
{{ .Files.Get "dashboards/aws-cur2-billing-dashboard.json" | indent 4 }}
